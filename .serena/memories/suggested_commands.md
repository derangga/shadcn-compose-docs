# Suggested Commands

## Development
- `bun install` — Install dependencies
- `bun run dev` — Start Vite dev server
- `bun run build` — Type-check (vue-tsc) and build for production
- `bun run preview` — Preview production build locally

## System Utilities (macOS/Darwin)
- `git` — Version control
- `ls`, `cd`, `find`, `grep` — Standard Unix utilities (macOS versions)

## Notes
- No linting, formatting, or testing commands are configured
- No pre-commit hooks detected
- Build command includes TypeScript type-checking via `vue-tsc -b`
